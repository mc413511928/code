第七天
格式化日期时分秒
	1要用new一个对象，日期和分秒都是new Date();
	2
时间戳
	1通过valueof()
	2getTime()
	3var date1=+nwa Date();返回大年总的毫秒数；
	4H5新增的获得总秒数Date.now();
数组的检测
	1instanceof  运算符
	2Array.isArray(参数)；优先于instanceof  ，H5新增的方法。IE9以上才支持，
数组添加方法
	1push()在我们数组末尾添加一个或者多个push推
·		1可以给数组追加新 的元素push()参数直接写数组元素就可以了
		2push完毕之后返回结果是新数组的长度；
	2unshift 在我们数组开头添加一个或者多个数组元素，
		1在数组前面追加新的元素
		2参数直接写 数组严肃就可以了
		3返回的是新数组的场地
		4原数组也会发生改变，
pop()删除数组最后一个元素
	1可以删除数组最后一个元素，只能删除一个元素，
	2pop()没有参数
	3返回的结果是被删除的那个元素
arr.shift 
	1可以删除数组的第一个元素，
	2shift()没有参数
	3返回的结果是被删除的元素
	4元数组夜壶发生变化，