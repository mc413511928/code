JS对象
变量属性函数方法的区别
	1变量 单独什么并赋值，使用的 时候直接写变量名 单独存在
	2属性在对象里面的不需要申明，使用时候对象名.属性
函数是单独申明的并且调用的函数名（）单独存在的
方法在对象里面调用：对象名.方法名（）：才可以调用，
利用object创建对象
	1var ojb=new Object(); 大写的O；
	2我们是利用等号赋值的方式添加属性和方法
	3每个属性和方法之间用分号隔开。
构造函数
	function 构造函数名（）{
	this.name;
	this.方法=function （）{}
}
new 构造名（）；
	1构造函数名字首字母要大写，
	2构造函数不需要return就可以返回结果，
	3我们调用构造函数必须使用new
	4每次创建一个对象都要new
构造函数 明星 泛指某一大类，它类似JAVA语言的 类 抽出的对象的公共部分，

2对象  特质 是一个具体的事物  
3我们利用构造函数创建对象我们成为对象实例化 
通过new关键字创建对象的过程我们成为对象实例化，
new关键字的 执行过程
	1new构造函数可以在内存中创建一个空的对象
	2this指向这个对象
	3执行构造函数里的代码给这个对象添加属性和方法
	4返回这个新的对象；所有不需要return.
变比对象
	1for(var k in 对象名）k没有单位 k变量输出的属性名，oJB[k]输出的是属性值
内置对象
	jS自带的对象，
查文档
	1mdn
Math.max
	1会把字符串里面的数字隐式转换成数字。
	2Math不是构造属性，所以我们不需要new，我们可以直接使用；
	3如果里面有非数字 返回的是NaN.如果没有参数返回都是 -infinity
绝对值
	1隐式转换会把字符“1”转换为数字型；
roud
	1四舍五入其他数字都是四舍五入但是.5特殊它往大了取。
random()
	1里面不跟参数，返回一个随机的小数大于等于0小于1
	2我们想要得到两个整数之间的随机整数并且包括这个2个整数
	function getRandomIntInclusive(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min + 1)) + min; //含最大值，含最小值 
}
使用DATE必须使用new
	1如果没有参数返回当前系统时间，
	2参数常用的写法，数字型 2019,10,01数字型会比实际大一个月或者是字符串型'2019-10-1 8:8；8，我们经常赢字符串；
getMonth()+1
	1比实际小1所以我们要+1；
getday 周一返回的是1周六返回的是6周日返回的是0；