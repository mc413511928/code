reverse()
	1翻转数组
sort()
	1数组排序
	2正序排序a-b,降序b-a



indexOf()
	1返回数组的索引方法。O要大写，从前面开始查找。
	2它返回第一个满足条件的索引号
	3如果没有该元素返回的是-1，
lastindexOf()
	1返回该数组 的索引号，从后面查找;
数组转换成字符串
	1toString();
	2join(分隔符)里面可以跟分隔符；
字符串对象
	1基本包装类型
		1把简单数据包装成为复杂数据类型
	2
字符串的不可变
	1字符的拼接还是 很占内存，
根据字符返回位置
	1str.indexOf( ‘要查找的字符’,[起始的位置] )
根据位置返回字符
	1charAt(索引号);根据位置返回字符
	3str[index]  H5新增用的最多，
	2charcodeAt()
3substr()
str.replace('a','b')他只会替换一个字符；
2把字符串转换数组
	split(分隔符)
简单数据类型
	5大数据类型都为简单数据类型；
	1null返回的是空的对象
	2如果有个变量我们打算储存为
	3简单数据类型储存在栈里面，复杂数据类型存储在堆里面，
	4简单数据类型存放在栈里面的，栈里面存放的是值
	5复杂数据类型首先在栈里面存放地址十六进制表示，然后