数组：就是一组数据的集合，储存在单个变量下的优雅方式
数组的创建
	利用new关键字 Arry（）
	2利用数组字面量创建数组[]
	3数组里面的数据是元素是逗号隔开的，
	4数组里面的元素是可以是任意数据类型，	
		数据类型有6种，对象也是数据类型，

数组的索引：
	1用来访问数组元素的序号，是从0开始的
	2访问数组元素：数组名[索引号]	.
	3因为没有这个数组元素，索引输出端 结果是 undefind;
遍历：
	1就是把数组中的每个元素从头到尾访问一次，
	2因为我们的数组索引是从0开始索引遍历数组的必须i=0；
数组的长度
	1数组的长度是元素的个数，不是索引号。
	2arr.length 动态监测数组元素的个数；
数组转换字符串；
新增数组元素
	1修改length长度没给值是undefind
	2追加数组元素。修改索引号，如果索引号被占有，就会替换原来的数组元素，
		不要给数组直接赋值，要加索引号；否则里面的数组元素都会消失，

	2
冒泡原来
	for(var i=0;i<arr.length-1;i++){
		for
函数：可以大量使用重复代码，
	1就是封装了一段可以被重复调用的代码块，就是让大量代码重复使用，
函数的使用
	1声明函数
		function什么函数的关键字全部小写
		2函数是做某件事函数名一般是动词
		3函数不调用，自己不执行，
	2调用函数：函数名（）；
函数的参数
	1形参，形参之间用逗号隔开。形式上的参数。在什么函数的小括号里面
		1接受实参的类似于一个变量
	2实参：在函数调用的小括号里面的是实参（实际参数）
	3函数的参数可以有，也可以没有，个数不限。
函数参数个数匹配
	1如果实参格式多于形参，会去到形参的个数
	2如果实参个数小于形参 多余的形参定义为undefind 最终的结果是NaN,
		1形参可以看做是不用申明的变量，是一个变量但是没有解释与值，结果是undefind,
	总结JS中函数的形参默认值是undefind,
	函数内部是不应该有输出语句的
函数的返回格式
	1只要函数遇到return就把后面的结果返回给函数的调用者，函数名（）=return后面的结果，