查漏补缺
1特殊情况如果盒子没有宽度（高度也是）padding不会撑开盒子
2border-collapse:collapse让我们的表格单元格，Th合并相连 的。
padding,margin写法：
	1,一个数值的时候是上下左右全是这个值，2个数值的时候就上下为第一个值,左右为第二个值
	  三个数值的时候，上，左右，下，】
	 四个数字的时候顺时针，上右下左的顺序，
圆角的做法：
	1正方行直接：border-radius:50%;也可以写宽度或者高度的一半。
	2长方形不能写百分比，必须写高度的一半，

盒子阴影：
	1box-shadow:水平阴影 垂直阴影 阴影尺寸 阴影颜色；
盒子水平居中：
	1让块级元素水平居中必须给宽，左右边距设置为anto;
	3行内元素尽量只设置左右边距，不设置上下边距，
	垂直方向外边距合并，解决方案只给一个盒子外边距，
	嵌套的关系外边距合并：
		1，给父盒子一个上边框，
		2给父盒子一个上padding。
		3给父亲添加overflow:hidden:
		浮动是让文字围绕着浮动的盒子，不会压着盒子。

1插入图片，产品图片，移动他们通过盒模型的padding,margin来实现，
	
2背景图片，只能通过background-position
 浮动会改变元素的display
	1浮动的元素默认相当于转换成行内块元素，
	2给需要浮动的元素添加一个标准流的父亲，
	3很多时候，父盒子不方便给高度，子盒子有多少内容，就显示多少内容，自动成家父盒子最为合理， 	
	4父盒子有高度，子盒子浮动，不会影响下面的标准流，
元素的隐藏于显示：
	1display：none隐藏元素，不占位置。
		display:block 也有让元素显示出来。
	2visibility把元素隐藏起来，位置还在，
响应式：
	清除浮动直接添加类名clearfix就可以了，h,p都有设置好的样式直接拿来使用，
pS切图 合并图层ctrl+e
JS部分：
	        // js的变量数据类型是只程勋在运行过程中，根据=右边的值来确定的，
		js是动态语言变量的数据类型是可以变化的。
	字符串
		字符串要用引号引起来.
		任何数据类型和字符串相加都是字符串；
		字符窜的拼接口诀：数值相加，字符相连，（重点）.
		变量和字符串的相连的口诀：隐隐++：
	数值类型，
	布尔数据类型：
		true参与运算当1
		false参与运算当0
	undefind+1最后的结果是NaN为这个不是一个数字。
	null空的
		null+pink"结果是nullpink
		null+1结果还是1.
冒泡排列；这是从大大小
冒泡排序：


        for (var i = 0; i < arr.length - 1; i++) {
            for (var j = 0; j <= arr.length - i - 1; j++) {
                if (arr[j] < arr[j + 1]) {
                    var temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }



使用event.stopPropagation()起到阻止捕获和冒泡阶段中当前事件的进一步传播。使用event.preventDefault()可以取消默认事件

	

	
、			