移动端布局
1流式布局
	1移动端真不要针对wenkit内核进行处理
2视口：
	1布局视口
	2视觉视口
	3理想视口：为了使网站在移动端有理想的浏览和阅读宽度而设定，
	mete视口标签的主要目的：布局视口宽度应该和理想视口的宽带一直，（苹果乔布斯发明），
	1mete标签的使用
		单标签
2二倍图
	1物理像素 ：分辨率 是真是存在的，
		PC端页面1px就是1px物理像素，
		移动端1px和端脑段1px不是一一对应关系！
		就是我们所说的分辨率
		b.在我们的ipone8里面1px是2个物理像素单位的。

	2物理像素比
	3背景的缩放
		1】background-size 
		只写了一个参数，肯定是宽度，高度省略了，会等比例缩放
		2】里面的单位可以跟%，相对于父盒子来说的，
		3】cover 要完全覆盖div盒子，可能有部分不骗显示不全，
		4】contain  高度和宽度等比例拉伸，当宽度或者高度铺满div盒子就不会拉伸了，就会出现部分空白。
		5】和backround简写的时候前面必须要有position 并且简写的时候要加/分隔开。position在前面，size在后面，
3移动端开发
	1】单独制作移动端页面（主流）
	2】响应式兼容pc移动端
		缺点：制作非常麻烦，需要花很大的经理去调兼容性
	总结：现在市场主流选择还是单独制作移动端页面，
4移动端技术解决方案；
	1】移动端浏览器主要以webkit内核为主，放心用h5标签。
	2】css初始化。
	3】CSS3盒子模型：
		1】	box-sizing:border-box;
			
			有了这句话就让盒子编程css3盒子模型
			padding和border不会再撑大盒子了。
			移动端放心的用 ， 刽有兼容问题。
5流式布局
	1】流式布局，百分比布局，有最大宽度最小宽度。根据屏幕
		图片回合文字的基线对齐，所有我门
		给子盒子添加上外边距会把父盒子带下来，所以我们给父盒子添加over flwol
		，加了固定定位的必须给宽带，

		